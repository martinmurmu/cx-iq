require 'spec_helper'

describe Limitation do
  before(:each) do
    @valid_attributes = {
      :user_id => 1,
      :my_lists => 1,
      :products_per_list => 1,
      :prm_reports => 1
    }
  end

#  it "should create a new instance given valid attributes" do
#    Limitation.create!(@valid_attributes)
#  end
end
